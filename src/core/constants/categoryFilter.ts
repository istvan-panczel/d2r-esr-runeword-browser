/**
 * Sentinel stored in `selectedCategories` to represent "no categories
 * selected" (as opposed to an empty array, which means "all categories").
 * Never a real category name coming from the parsed data.
 */
export const NO_CATEGORIES_MARKER = '__none__';
